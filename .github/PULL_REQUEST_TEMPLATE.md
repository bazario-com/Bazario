## What changed

<!-- Brief description of the change and why -->

## Checklist

- [ ] `npm run test:cov` passes locally in `apps/api`
- [ ] `npx prisma validate` passes (if `schema.prisma` changed)
- [ ] `npm run build` passes in both `apps/api` and `apps/web`
- [ ] New/changed endpoints or pages are reflected in `apps/api/README.md` / `apps/web/README.md`
- [ ] Any new Prisma relation has a back-reference on both sides (see `ARCHITECTURE.md`)

## Screenshots (for UI changes)

<!-- Optional -->
